#!/bin/bash -l
set -euo pipefail

# ==== ONLY EDIT WITHIN THIS BLOCK =====

export PANGEO_ENV="openscapes"

mkdir -p $HOME/Desktop

cp $REPO_DIR/*.desktop $HOME/Desktop/

# ==== ONLY EDIT WITHIN THIS BLOCK =====

exec "$@" --ResourceUseDisplay.track_cpu_percent=True
